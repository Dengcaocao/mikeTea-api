// const mongoose = require('mongoose')

// const baseModel = mongoose.Schema({
  
// })

module.exports = {
  createdAt: {
    type: Date,
    default: Date.now
  },
  updatedAt: {
    type: Date,
    default: Date.now
  }
}
